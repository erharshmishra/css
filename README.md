# css

## CSS Learning Repository

Welcome to my CSS Learning Repository! In this repository, I’ve compiled all the CSS topics and concepts I have learned over time. Each file and folder represents a specific concept, practice, or project that I worked on to strengthen my understanding of CSS.

This repository is a mix of exercises, assignments, and mini-projects that helped me gain hands-on experience in web styling and layout. Below, you’ll find a detailed explanation of each folder and file within this repository.

## Table of Contents

| **File Name**                | **Topic**                   | **Explanation**                                                                                                                                      |
|------------------------------|-----------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------|
| **45.Inline_Styles**          | Inline Styles               | Demonstrates how to apply CSS directly within HTML elements using the `style` attribute. This is quick but not scalable for large projects.           |
| **46.Internal_Styles**        | Internal CSS                | Shows how to define CSS rules within the `<style>` tag in the `<head>` section of an HTML document. Ideal for single-page styling.                    |
| **47.External_Styles**        | External CSS                | Explains how to link an external CSS file to an HTML document. Best for styling multiple pages consistently.                                          |
| **48.Color_Property**         | Color Property              | Demonstrates how to set text and background colors using different formats like color names, hex, RGB, and RGBA.                                      |
| **49.Background_Color_Property** | Background Color Property  | Explains how to apply background colors to HTML elements using the `background-color` property.                                                     |
| **52.Text_Align**             | Text Alignment              | Covers how to align text inside elements with values like `left`, `center`, `right`, and `justify`.                                                    |
| **53.Font_Weight**            | Font Weight                 | Explains how to control the thickness of text using `font-weight`, with values like `normal`, `bold`, and `lighter`.                                   |
| **54.Text_Decoration**        | Text Decoration             | Demonstrates how to add text decorations like `underline`, `line-through`, and `overline` to enhance the styling of text.                             |
| **55.Line_Height**            | Line Height                 | Defines the space between lines of text, improving readability and the overall design of text blocks.                                                 |
| **56.Letter_Spacing**         | Letter Spacing              | Adjusts the spacing between individual letters in a text element, helping with typography and legibility.                                              |
| **57.Font_Size**              | Font Size                   | Specifies the size of the text using various units like `px`, `em`, `rem`, and `%`. Useful for responsive design.                                   |
| **58.Font_Family**            | Font Family                 | Defines the type of font to apply to elements. It allows you to choose from system fonts or web-safe fonts for better typography.                     |
| **61.Universal_Selector**     | Universal Selector          | Selects all elements on the page with the `*` selector. Often used for resetting styles or applying global styling.                                   |
| **62.Element_Selector**       | Element Selector            | Targets elements based on their tag name (e.g., `div`, `p`, `h1`). Commonly used for applying styles to all elements of that type.                  |
| **63.Id_Selector**            | Id Selector                 | Selects elements by their unique `id` attribute, useful for targeting specific elements for styling.                                                  |
| **64.Class_Selector**         | Class Selector              | Selects elements by their `class` attribute. This is used to apply styles to multiple elements with the same class.                                   |
| **66.Descendant_Selector**    | Descendant Selector         | Targets elements that are nested inside other elements. Useful for more specific styling in a nested structure.                                        |
| **67.Adjacent_Sibling_Combinator** | Adjacent Sibling Combinator | Selects elements that are immediately preceded by a sibling element. Useful for styling elements that appear next to each other.                       |
| **68.Child_Combinator**       | Child Combinator            | Selects direct child elements within a parent. It helps to style immediate descendants only.                                                          |
| **69.Attribute_Selector**     | Attribute Selector          | Targets elements based on the presence or value of an attribute (e.g., `input[type="text"]`).                                                        |
| **71.Pseudo_Class**           | Pseudo-Class                | Allows you to select elements based on their state or position, like `:hover`, `:focus`, or `:nth-child()`.                                           |
| **72.Pseudo_Elements**        | Pseudo-Elements             | Targets parts of an element, such as `::before` or `::after`, to add style to content without modifying the HTML structure.                           |
| **73.Cascading_And_Specificity** | Cascading and Specificity    | Explains the CSS cascade and specificity rules, which determine which styles apply when multiple rules conflict.                                       |
| **75.!important**             | !important                  | Describes how to use the `!important` flag to override other CSS rules, though it should be used sparingly to avoid confusion in styling.             |
| **76.Inheritance**            | Inheritance                 | Discusses how CSS properties are inherited by child elements from their parent elements, and how to override or prevent inheritance.                 |
| **78.Height_And_Width**       | Height and Width            | Demonstrates how to set the height and width of elements, both in fixed and dynamic units like `px`, `%`, `em`, and `auto`.                            |
| **79.Border_And_Border_Radius** | Border and Border Radius    | Explains how to add borders to elements and create rounded corners using the `border-radius` property for aesthetic design.                            |
| **80.Padding**                | Padding                     | Defines space between an element's content and its border. Padding helps to position content inside an element.                                        |
| **81.Margin**                 | Margin                      | Controls the space outside an element's border. It's useful for positioning elements and controlling spacing between them.                            |
| **83.Display_Property**       | Display Property            | Explains how the `display` property works in CSS. It helps control the behavior of an element, such as `block`, `inline`, `flex`, `grid`, etc.        |
| **84.Units**                  | Units                       | Discusses various CSS units like `px`, `em`, `rem`, `%`, and more, explaining when and why to use them for dimensions and positioning.                |
| **85.Drawback_of_Em**         | Drawbacks of `em`           | Explains the limitations of using `em` units for sizing, especially when applied in nested elements, which may cause unexpected results.               |
| **87.Alpha_And_Opacity**      | Alpha and Opacity           | Covers how to use RGBA values and the `opacity` property to adjust the transparency of elements. Useful for layering and fading effects.              |
| **88.Transition**             | Transition                  | Introduces CSS transitions, allowing you to animate changes in properties over a given duration for smooth visual effects.                             |
| **89.Transform**              | Transform                   | Explains how to apply transformations like scaling, rotating, translating, and skewing to elements, enabling creative visual effects.                  |
| **90.Box_Shadow**             | Box Shadow                  | Demonstrates how to add shadows to elements with the `box-shadow` property, creating a sense of depth and focus in the design.                         |
| **91.Background_Image**       | Background Image            | Shows how to add images as backgrounds to elements, as well as control their size, positioning, and repetition.                                          |
| **92.Card_Hover_Effect**      | Card Hover Effect           | A project showing how to create interactive hover effects on a card element, a common pattern in modern web design.                                      |
| **93.Position**               | Position                    | Explains how to use the `position` property to control element positioning (`static`, `relative`, `absolute`, `fixed`, `sticky`).                     |
| **94.Simple_Smiley_Face**     | Simple Smiley Face          | A fun project where I created a smiley face using CSS properties like `border-radius`, `background-color`, and `box-shadow`.                        

## How to Use

1. Clone the repository to your local machine:
    ```bash
    git clone https://github.com/your-username/css-learning-repository.git
    ```
2. Explore the individual files and folders for different CSS topics.
3. Open any HTML file in your browser to see the CSS styles applied in action.

## Learning Purpose

The main goal of this repository is to demonstrate my learning journey with CSS and the various techniques and features that I have explored. The exercises here focus on:

- Understanding the fundamental CSS properties such as `color`, `font-size`, `background`, `margin`, `padding`, and others.
- Learning how to style websites using both simple and advanced CSS techniques.
- Mastering the layout techniques like Flexbox and Grid to create responsive, modern web designs.
- Experimenting with animations, transitions, and transforms to make webpages interactive and dynamic.
- Using CSS selectors and pseudo-classes to target elements efficiently and make styling more specific and controlled.
- Building mini-projects to apply my CSS knowledge in practical ways.

## Author
- **Name**: Harsh Mishra 
- **Email**: harshmishra8103@gmail.com

> By sharing these files, I hope to showcase my progress and potentially help others who are starting out with CSS. Feel free to clone, fork, or modify any of the examples to learn and experiment on your own!
